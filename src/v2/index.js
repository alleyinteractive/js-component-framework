/**
 * This is the legacy export, available to projects coming from v1 & v2 for
 * which a more complete upgrade isn't immediately feasible.
 */
import Component from './Component';
import initComponents from './initComponents';

export {
  Component,
  initComponents,
};
